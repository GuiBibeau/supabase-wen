{
  "name": "wen",
  "description": "wen monorepo",
  "private": true,
  "workspaces": [
    "app",
    "api",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --scope=app --include-dependencies --no-deps",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "@types/node": "^17.0.14",
    "turbo": "^1.1.6"
  }
}
